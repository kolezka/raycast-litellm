# Raycast build output
dist/
.raycast/

# Regenerated by every `ray build` and `ray lint`. Not consumed here: this
# extension declares ExtensionPreferences itself (see a0589db) rather than
# using the global this file generates.
raycast-env.d.ts

# Dependencies
node_modules/

# Claude Code worktrees and subagent-driven-development scratch
.claude/worktrees/
.superpowers/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Never commit secrets — the LiteLLM API key lives in Raycast preferences only
.env
.env.*
*.pem

# Written by `kb add` — per-repo KG_ROOTS for the world graph. Machine-local.
.envrc

# Machine-local code graph cache
graphify-out/

# Internal planning docs
docs/superpowers/
